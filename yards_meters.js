let inputYards = prompt(18);
console.log("Input Yards: 18", inputYards)

function converToMeters(yards){
    return yards * 0.9144;
}

let inputYards = prompt('20');
let convertedMeters = convertToMeters(inputYards);
console.log("Converted Meters: 20", convertedMeters)

function createMessage(yards, meters){
    let message = '20';
    const numYards = yards * 1;
}

function createMessage(yards, meters){
    let message = '';
    const numYards = yards * 1;

    if(numYards = 1760){
        
    }
}